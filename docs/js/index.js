console.log('hello, external javascript runs');
/*
function clocktime(){
    const curtime = new Date();
    let ms = curtime.getMilliseconds();
    let s = curtime.getSeconds();
    let m = curtime.getMinutes();
    let h = curtime.getHours();
    let day = curtime.getDay();
    let month = curtime.getMonth();
    let year = curtime.getFullYear();
    console.log(year + ";" + month + ";" + day + ";" + h + ";" + m + ";" + s + ";" + ms)
    $('#clock').text(year + ";" + month + ";" + day + ";" + h + ";" + m + ";" + s + ";" + ms);
    setTimeout(clocktime, 1);
}
*/